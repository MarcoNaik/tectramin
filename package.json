{
  "name": "tectramin",
  "private": true,
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "clean": "turbo run clean && rm -rf node_modules",
    "format": "prettier --write \"**/*.{ts,tsx,js,jsx,json,md}\" --ignore-path .gitignore",
    "android": "expo run:android",
    "ios": "expo run:ios",
    "build:ios": "cd apps/native && eas build -p ios --profile production --local",
    "build:ios:simulator": "cd apps/native && eas build -p ios --profile production-simulator --local",
    "build:android": "cd apps/native && ANDROID_HOME=/opt/homebrew/share/android-commandlinetools JAVA_HOME=$(/usr/libexec/java_home -v17) eas build -p android --profile production --local",
    "build:android:apk": "cd apps/native && ANDROID_HOME=/opt/homebrew/share/android-commandlinetools JAVA_HOME=$(/usr/libexec/java_home -v17) eas build -p android --profile production-apk --local",
    "prod:ios": "turbo run prod:ios",
    "prod:android": "turbo run prod:android",
    "prod": "turbo run prod:ios prod:android --parallel"
  },
  "devDependencies": {
    "prettier": "3.5.3",
    "turbo": "2.5.2"
  },
  "engines": {
    "node": ">=18.8.0"
  },
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "packageManager": "npm@10.8.0",
  "dependencies": {
    "@date-fns/utc": "^2.1.1",
    "date-fns": "^4.1.0",
    "expo": "~54.0.31",
    "react": "19.1.0",
    "react-native": "0.81.5",
    "react-native-worklets": "^0.7.1"
  },
  "version": "1.0.0"
}
